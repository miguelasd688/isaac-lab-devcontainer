#!/bin/bash

set -e

echo "source /opt/ros/humble/setup.bash" >> ~/.bashrc

# here you move to your custom asset and install it so you can start working with it directly. 
#cd extrange_robot
#python -m pip install -e source/extrange_robot/
